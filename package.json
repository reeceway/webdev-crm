{
  "name": "webdev-crm",
  "version": "1.0.0",
  "description": "Open-source CRM for web development businesses",
  "scripts": {
    "start": "cd backend && node src/index.js",
    "build": "cd frontend && npm install && npm run build",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["crm", "web-development", "business", "open-source"],
  "license": "MIT"
}
